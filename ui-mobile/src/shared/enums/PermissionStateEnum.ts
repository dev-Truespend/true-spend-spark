export enum PermissionStateEnum {
  NotDetermined = "not_determined",
  Denied = "denied",
  Allowed = "allowed",
  AllowedOnce = "allowed_once",
  AllowedWhileUsing = "allowed_while_using",
  AllowedAlways = "allowed_always"
}
