/* region: archive — manual transaction edit (removed from MVP)
 *
 * The /transactions/[id]/edit route is archived. A null component is exported
 * so Expo Router still resolves the path; the screen is empty if reached.
 *
 * import { TransactionFormScreen } from "@/features/transactions/screens/TransactionFormScreen";
 * export default TransactionFormScreen;
 *
 * endregion */

export default function ArchivedTransactionsEditRoute() {
  return null;
}
